# Formatting for a single parameter

Formatting for a single parameter

## Usage

``` r
um.help(X)

um.help
```

## Format

An object of class `function` of length 1.

## Arguments

- X:

  would be one slot from within unmarkedFit@estimates list

## Value

a data.frame of the proccess, parameter, SE and approximate Z and P
values (assuming normal distribution)

## Examples

``` r
No
#> Error: object 'No' not found
```
